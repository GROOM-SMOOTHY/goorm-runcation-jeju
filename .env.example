# ===========================================
# Supabase 환경 변수 (프론트엔드에서 사용)
# ===========================================
# 1. https://supabase.com/dashboard 에서 프로젝트 선택
# 2. 왼쪽 메뉴 [Settings] → [API] 이동
# 3. 아래 두 값을 복사해 .env 파일에 붙여넣기
#    (이 파일을 .env 로 복사한 뒤 실제 값으로 채우면 됨)
# ===========================================

# 프로젝트 URL (Project URL)
# 예: https://abcdefgh.supabase.co
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# 익명(공개) API 키 (anon public key)
# 클라이언트에서 사용해도 되는 키 (Row Level Security로 보안 관리)
# service_role 키는 절대 여기 넣지 말 것
VITE_SUPABASE_ANON_KEY=your-anon-key
